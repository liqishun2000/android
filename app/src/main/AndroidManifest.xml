<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--计步器-->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!--通知权限-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Android">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.android.training.permission.PermissionActivity" />
        <activity android:name="com.example.android.training.compose.common.CommonActivity"/>
        <activity android:name="com.example.android.training.compose.statusbar.StatusBarTrainingActivity" />
        <activity android:name="com.example.android.training.xml.CoordinatorLayoutTrainingActivity" />
        <activity android:name="com.example.android.training.xml.ClipTrainingActivity" />
    </application>

</manifest>